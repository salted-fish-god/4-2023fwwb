/* 学生修改个人信息界面 */
import React from "react";

interface ReviseProps{}

interface ReviseState{}

function Revise(props: ReviseProps, state: ReviseState){
    return (
        <div className="Revise">
            <h1>Revise</h1>
        </div>
    )
}

export default Revise;